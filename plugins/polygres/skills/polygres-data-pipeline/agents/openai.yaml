interface:
  display_name: "Polygres Data Pipeline"
  short_description: "Set up Polygres sync, ingestion, and retrieval"
  default_prompt: "Use $polygres-data-pipeline to inspect my source, choose managed PostgreSQL sync or the smallest safe custom pipeline, and verify the selected ingestion and retrieval path."
